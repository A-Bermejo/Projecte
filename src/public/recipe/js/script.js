// VARIABLES GLOBALES
var opcionsOpen = false;

async function init() {
    var nombre = document.getElementById("nombre");

    // const url = "http://localhost:3050/recipe/1"

    // fetch(url)
    //     .then(res => res.json())
    //     .then(data => {
    //         console.log(data);
    //         nombre.textContent = data.nom_recepta
    //     })
}

$(document).ready(init);